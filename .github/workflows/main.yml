name: first github action 

on: 
  push:
    branches: 
      main

jobs:
  first-job:
    runs-on:
      ubuntu-latest

    steps:
      - name: print a welcome message
        run: echo "hello world"

      - name: checkout repo
        uses: actions/checkout@v4.2.2

      - name: making sure it is there 
        run: ls -la
        
  
